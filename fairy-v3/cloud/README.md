# Fairy Cloud

This boundary will contain the FastAPI composition root, PostgreSQL 18
adapters, S3-compatible object storage, lease/outbox workers, OIDC PKCE, and
Docker Compose development services. Domain behavior remains in `core/`.
