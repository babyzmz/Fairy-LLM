{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "companion-window",
  "description": "Realtime-only secondary window; Rust permits read-only local readiness and enforces dedicated RPC, capture-list, provider-status, worker, and voice scopes",
  "windows": ["companion"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-close",
    "core:window:allow-hide",
    "core:window:allow-set-focus"
  ]
}
