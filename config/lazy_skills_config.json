{
  "use_lazy_skills": true,
  "enable_debug_logging": true,
  "graceful_fallback": true,
  "prefer_realtime": true,
  "realtime_timeout_sec": 8,
  "web_research_timeout_sec": 15,
  "_comments": {
    "use_lazy_skills": "Enable the canonical bundle runtime",
    "enable_debug_logging": "Enable detailed logging for skill routing and execution",
    "graceful_fallback": "Allow bundle-local degradation within the canonical runtime path",
    "prefer_realtime": "Route real-time queries (weather, rates, etc.) to realtime_lookup first",
    "realtime_timeout_sec": "Timeout for realtime_lookup operations",
    "web_research_timeout_sec": "Timeout for web_research operations"
  },
  "_runtime_notes": {
    "pipeline": "bundle_runtime",
    "skills_root": "app/skills/bundles/",
    "router_location": "app/lazy_skill_router/",
    "realtime_execution": "deterministic_direct_path"
  }
}
