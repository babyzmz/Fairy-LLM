"""Tools package — low-level capability adapters.

Tools are NOT skills and NOT agents.
They provide:
  browser/    — browser automation wrappers
  web/        — HTTP clients, search adapters
  rendering/  — card/text rendering primitives
  extraction/ — DOM, text, numeric extractors
  system/     — OS / filesystem access wrappers
"""
